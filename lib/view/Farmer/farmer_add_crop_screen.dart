import 'package:flutter/material.dart';

class FarmerAddCropScreen extends StatelessWidget {
  const FarmerAddCropScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}